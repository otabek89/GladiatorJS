class Gladiator
    constructor(name, weapon) {
        this.name = name;
        this.weapon = weapon;
    }
    if (Gladiator.weapon !== ("Spear" || "Club" || "Trident"); {
        alert("This ain't that kinda fight.")
    }
}